import { Button } from "@ui5/webcomponents-react"

const Launchpad = () => {
        return (
                <>
                    <div

                    style={{height: "calc(100vh - 304px)"}}
                    >
                        <Button>This is FLP</Button>
                    </div>
                </>
  )
};

export default Launchpad;
